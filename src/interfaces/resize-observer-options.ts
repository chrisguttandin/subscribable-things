import { TResizeObserverBoxOptions } from '../types';

// @todo TypeScript does not include type definitions for the Resize Observer specification yet.
export interface IResizeObserverOptions {
    box?: TResizeObserverBoxOptions;
}
