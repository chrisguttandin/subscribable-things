import { mediaQueryMatches } from '../../src/module';

describe('module', () => {

    it('should export the mediaQueryMatches function', () => {
        expect(mediaQueryMatches).to.be.a('function');
    });

});
